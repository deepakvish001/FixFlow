interface:
  display_name: "FixFlow"
  short_description: "Repo-specific patterns and workflows for FixFlow"
  default_prompt: "Use the FixFlow repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true